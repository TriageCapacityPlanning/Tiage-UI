# Triage-UI Testing Documentation

## Setup

Clone the repo and then install necessary node modules using `npm install`

## Running unit tests
Run `npm test`

## Code coverage documentation
Run `ng test --no-watch --code-coverage`
